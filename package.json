{
   "//": "To install package: npm install pakage-name -w folder-name, to check updates = npx npm-check-updates -u -w",
   "name": "realestate-monorepo",
   "private": true,
   "workspaces": [
      "backend",
      "frontend"
   ],
   "scripts": {
      "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\" -n \"B,F\" -c \"yellow,cyan\"",
      "build": "npm run build --workspace=frontend",
      "start": "npm run start --workspace=backend",
      "install:all": "npm install",
      "clean": "rd /s /q node_modules 2>nul && npm run clean --workspaces"
   },
   "devDependencies": {
      "@vitejs/plugin-react": "^5.1.2",
      "concurrently": "^9.2.1"
   },
   "dependencies": {
      "cors": "^2.8.5"
   }
}
